package main

import "fmt"

func main() {
	msg, age, cost := "sup Dog", 21, 19.99
	fmt.Printf("Var msg is has this value %v, and the type of %T\n", msg, msg)
	fmt.Printf("Var age is has this value %v, and the type of %T\n", age, age)
	fmt.Printf("Var cost is has this value %v, and the type of %T\n", cost, cost)
}
